@model IEnumerable<DAL.Autores>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nombre_autor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.apellidos_autor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nombre_autor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.apellidos_autor)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_autor">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_autor">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_autor">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
