@model IEnumerable<DAL.Colecciones>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nombre_coleccion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nombre_coleccion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_coleccion">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_coleccion">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_coleccion">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
