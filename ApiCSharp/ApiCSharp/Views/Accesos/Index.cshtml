@model IEnumerable<DAL.Accesos>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.codigo_acceso)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.descripcion_acceso)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.codigo_acceso)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.descripcion_acceso)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_acceso">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_acceso">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_acceso">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
